## 我的总体建议

**不要一上来就“完整重写网页版”。** 先做一个 Qt/C++ 的核心 MVP：MIDI 输入、音符识别、曲谱加载、播放进度、练习判断、基础钢琴卷帘窗。音色、AI 评分、曲谱编辑、复杂 UI 可以后置。

我会建议你按这个架构做：

```
SmartPianoQt/
├── app/                 # main.cpp, Application bootstrap
├── core/                # NoteEvent, Song, PlaybackEngine, PracticeEngine
├── midi/                # MidiInputService, YamahaFilter
├── audio/               # SynthEngine / FluidSynthEngine / SamplePlayer
├── parser/              # MidiParser, MusicXmlParser, JsonSheetParser
├── storage/             # SQLiteRepository
├── ui/                  # QML 或 Widgets 界面
└── tests/               # 解析、节拍、练习判断单元测试
```

## 1. UI：建议优先用 Qt Quick/QML，而不是传统 Widgets

你的项目有大量动态渲染：琴键高亮、卷帘窗、音符下落、播放线、曲谱滚动。你自己在优化总结里也提到过，网页版 DOM 渲染大量音符会卡，建议改成 Canvas 并且只绘制可见区域。迁移到 Qt 后同理：不要用一堆 `QLabel/QPushButton` 去表示每个音符，应该用一个自定义绘制区域，只画当前视口内的音符。

推荐两种路线：

**路线 A：Qt Quick/QML + C++ 后端。**
 适合做现代界面、动画、缩放、卷帘窗、触控交互。C++ 负责 MIDI、解析、播放状态机，QML 负责显示。

**路线 B：QWidget + QPainter/QGraphicsView。**
 适合你想纯 C++、界面不追求很现代的情况。也可以做得很好，但动画和视觉效果会比 QML 麻烦一些。

我个人推荐 **Qt Quick/QML + C++ Core**。

## 2. MIDI：不要自己硬啃系统 API，建议用 RtMidi

Qt Multimedia 主要覆盖音频、视频、摄像头、录制、原始音频输入输出等能力，不是专门的 MIDI 输入库；Qt 官方文档里 Qt Multimedia 的 C++ 部分也是围绕播放、录音、音频输出等展开。

MIDI 输入建议直接用 **RtMidi**。它是 C++ 库，提供 `RtMidiIn` / `RtMidiOut`，支持 Windows、macOS、Linux，返回原始 MIDI 字节，正好适合你现在的 `midiController.js` 逻辑迁移。

你现有项目里有一个很重要的 Yamaha PSR-E383 专属逻辑：过滤 `254` 和 `248`，避免心跳包、时钟信号导致误触发。这个一定要原样迁移到 C++。

可以写成类似这样：

```
void MidiInputService::handleMessage(const std::vector<unsigned char>& msg)
{
    if (msg.empty()) return;

    const unsigned char status = msg[0];

    // Yamaha PSR-E383：过滤 Active Sensing / Timing Clock
    if (status == 0xFE || status == 0xF8) {
        return;
    }

    if (msg.size() < 3) return;

    const unsigned char cmd = status & 0xF0;
    const int note = msg[1];
    const int velocity = msg[2];

    if (cmd == 0x90 && velocity > 0) {
        emit noteOn(note, velocity);
    } else if (cmd == 0x80 || (cmd == 0x90 && velocity == 0)) {
        emit noteOff(note);
    }
}
```

## 3. 音频：钢琴音色建议用 FluidSynth，不建议一开始自己写合成器

网页版现在用 Soundfont Player 加载 `acoustic_grand_piano`，Qt/C++ 里最自然的替代方案是 **FluidSynth + `.sf2` 音色文件**。FluidSynth 本身就是基于 SoundFont 2 的实时软件合成器，支持 Linux、macOS、Windows，也可以作为共享库嵌入应用。

你可以这样设计：

```
AudioEngine 接口
├── playNote(midi, velocity)
├── stopNote(midi)
├── setVolume(value)
├── loadSoundFont(path)
└── panic()  # 关闭所有音
```

实现类：

```
FluidSynthEngine : public AudioEngine
```

如果你后面想自己混音、做采样播放器，可以用 Qt 的 `QAudioSink`。Qt 6.11 的 `QAudioSink` 已有 callback-based 低延迟接口，但实时音频回调里不能阻塞、不能加锁、不能分配内存，否则会有爆音风险。

实际项目里我会这样选：

```
只想有好听钢琴音色：FluidSynth
想研究底层音频/采样播放器：QAudioSink / PortAudio
想做专业音乐软件级别：JUCE 也值得看，但它就不是纯 Qt 生态了
```

## 4. 数据模型：从一开始就用 tick，不要只用 beat 或毫秒

你现在的 MIDI 解析会把多轨事件合并、按绝对时间排序，并把音符转成 `startTimeBeat` 和 `durationBeat`。这个思路可以保留，但 C++ 版建议内部统一用 **tick**，显示和播放时再转换成秒。

建议核心模型：

```
struct NoteEvent {
    int midi = 60;
    int velocity = 80;

    qint64 startTick = 0;
    qint64 durationTick = 0;

    int track = 0;
    int channel = 0;
    int fingering = 0;

    QString noteName;   // C4, F#4
    QString hand;       // L / R，可选
};

struct TempoEvent {
    qint64 tick = 0;
    int bpm = 120;
};

struct Song {
    QString title;
    int ppq = 480;
    QVector<TempoEvent> tempoMap;
    QVector<NoteEvent> notes;
};
```

这样后面支持变速、变拍号、多轨、左右手、踏板、循环练习都会容易很多。

## 5. 曲谱解析：顺手修掉 `.mxl` 支持问题

README 里写支持 MusicXML、压缩 MusicXML `.mxl`、MIDI、JSON；但当前 `parseSheetFile` 的分支只看到 `.xml`、`.mid/.midi`、`.json`，没有 `.mxl`。迁移时建议明确支持 `.mxl`，否则文档和功能会不一致。

C++ 里可以这样做：

```
.xml   -> tinyxml2 / pugixml
.mxl   -> libzip / QuaZip 解压后读取 MusicXML
.mid   -> 自己写解析器或用 MIDI 文件库
.json  -> QJsonDocument
```

MusicXML 解析建议先支持这些字段：

```
pitch: step / alter / octave
duration
voice
staff
measure
tempo
time signature
fingering
chord
rest
```

MIDI 解析建议先支持：

```
Note On / Note Off
Tempo meta event
Time signature meta event
Program change
Sustain pedal CC64
多轨合并
running status
```

## 6. 本地曲谱库：Qt 版建议直接用 SQLite

网页版用 IndexedDB 保存曲谱，这是合理的；Qt 桌面版建议换成 SQLite。Qt 自带 SQL 模块，配合 SQLite 很方便。

建议表结构：

```
sheets(
    id INTEGER PRIMARY KEY,
    title TEXT,
    composer TEXT,
    source_format TEXT,
    file_hash TEXT UNIQUE,
    bpm INTEGER,
    ppq INTEGER,
    created_at TEXT,
    updated_at TEXT
);

notes(
    id INTEGER PRIMARY KEY,
    sheet_id INTEGER,
    midi INTEGER,
    start_tick INTEGER,
    duration_tick INTEGER,
    velocity INTEGER,
    track INTEGER,
    channel INTEGER,
    fingering INTEGER
);

practice_sessions(
    id INTEGER PRIMARY KEY,
    sheet_id INTEGER,
    started_at TEXT,
    duration_seconds INTEGER,
    score INTEGER,
    wrong_notes INTEGER,
    missed_notes INTEGER
);

practice_events(
    id INTEGER PRIMARY KEY,
    session_id INTEGER,
    expected_midi INTEGER,
    actual_midi INTEGER,
    expected_tick INTEGER,
    actual_time_ms INTEGER,
    result TEXT
);
```

这样你的“智能”部分后面就有数据来源了，不只是即时判断对错。

## 7. “智能钢琴助手”的智能点可以这样做

我觉得你这个项目最有价值的“智能”不是聊天，而是练琴反馈。

第一阶段做 **实时判定**：

```
当前应该弹什么音
用户实际弹了什么音
是否提前 / 滞后
是否错音
是否漏音
是否多弹
按键持续时间是否太短
```

第二阶段做 **练习报告**：

```
全曲准确率
节奏稳定性
最容易错的小节
最容易错的音程
左手 / 右手错误分布
建议重复练习的片段
```

第三阶段做 **自适应练习**：

```
错得多的地方自动加入 A-B 循环
连续错 3 次自动降速
连续正确后自动恢复速度
难点小节生成专项练习
```

第四阶段再考虑 **AI 总结**：

```
“你在第 12-16 小节右手 G4 到 B4 的跳进经常偏早，建议先 60 BPM 分手练习。”
```

这样“智能”会很实用，而不是为了 AI 而 AI。

## 8. 播放状态机一定要独立出来

现在 `app.js` 是核心控制中枢，负责 UI、播放状态机、练习模式、节拍器和进度条。Qt 版里建议把状态机从 UI 里抽出来，否则后面会很难维护。

建议拆成：

```
PlaybackEngine
├── loadSong(song)
├── play()
├── pause()
├── stop()
├── seek(tick)
├── setBpmScale(0.5 / 1.0 / 1.2)
└── currentTick()

PracticeEngine
├── setMode(AutoPlay / WaitForNote / RhythmPractice)
├── onUserNoteOn(midi, velocity, timestamp)
├── onUserNoteOff(midi, timestamp)
├── currentExpectedNotes()
└── resultStream()
```

UI 只订阅信号：

```
connect(playback, &PlaybackEngine::positionChanged, pianoRoll, &PianoRoll::setCurrentTick);
connect(practice, &PracticeEngine::noteJudged, scorePanel, &ScorePanel::appendResult);
```

## 9. 渲染：卷帘窗只画可见音符

不要每个音符一个控件。建议维护一个已排序的 `notes` 数组，根据当前 tick 和缩放比例计算可见范围：

```
visibleStartTick = currentTick - lookBehindTicks;
visibleEndTick   = currentTick + lookAheadTicks;
```

然后二分查找只画这个范围内的音符。

伪逻辑：

```
void PianoRollItem::paint(QPainter* p)
{
    auto visibleNotes = song->notesInRange(visibleStartTick, visibleEndTick);

    for (const auto& note : visibleNotes) {
        QRectF r = noteToRect(note);
        p->drawRoundedRect(r, 4, 4);
    }

    drawPlayhead(p);
    drawKeyboardGuide(p);
}
```

这样即使一首曲子有几万音符，界面也不会因为控件数量爆炸而卡死。

## 10. 可以做一个“迁移对应表”

| 网页模块            | Qt/C++ 模块                                        | 建议                                          |
| ------------------- | -------------------------------------------------- | --------------------------------------------- |
| `audioEngine.js`    | `FluidSynthEngine`                                 | 用 `.sf2` 音色，保留 `playNote/stopNote` 接口 |
| `midiController.js` | `MidiInputService`                                 | 用 RtMidi，保留 Yamaha `0xFE/0xF8` 过滤       |
| `parser.js`         | `SheetParser`                                      | MIDI/MusicXML/JSON 分开实现                   |
| `sheetLibrary.js`   | `SheetRepository`                                  | IndexedDB 换 SQLite                           |
| `noteMap.js`        | `NoteMap`                                          | 可直接移植成 C++ 静态函数                     |
| `app.js`            | `PlaybackEngine + PracticeEngine + MainController` | 不要把 UI 和业务逻辑混在一起                  |
| DOM/Canvas UI       | `PianoRollItem / KeyboardItem`                     | 只绘制可见区域                                |

## 11. 我建议你的 Qt 版 MVP 做到这些就够了

```
1. 能识别 Yamaha PSR-E383 的 MIDI 输入
2. 能显示 88 键或 61 键虚拟键盘，并实时高亮
3. 能导入 MIDI 文件并显示卷帘窗
4. 能播放曲子并同步高亮
5. 能进入练习模式：弹对当前音才继续
6. 能保存曲谱到 SQLite
7. 能统计正确、错误、漏弹
```

这个 MVP 做出来后，你就已经比普通“虚拟钢琴”强很多了。

## 12. 技术选型建议

我会这样选：

```
Qt 版本：Qt 6.8 LTS 或更新版本
UI：Qt Quick/QML
MIDI：RtMidi
音源：FluidSynth + SF2
数据库：SQLite
XML：pugixml 或 tinyxml2
MXL：libzip 或 QuaZip
构建：CMake
测试：Qt Test 或 Catch2
```

如果你更看重长期稳定，Qt 6.8 LTS 是合适的基线；Qt 官方说明从 Qt 6.8 开始 LTS 维护周期为 5 年。

## 结论

你的方向是对的。这个项目迁移到 Qt/C++ 后，最大的提升会是：MIDI 更稳定、离线体验更好、音频延迟更可控、曲谱库更像正式软件、练习数据可以长期积累。

我最建议你先做这三个核心模块：

```
MidiInputService 负责真实琴输入
PlaybackEngine 负责播放和节拍
PracticeEngine 负责判断对错
```

只要这三块写干净，后面无论是卷帘窗、评分系统、AI 练习建议，还是曲谱编辑器，都能自然扩展。