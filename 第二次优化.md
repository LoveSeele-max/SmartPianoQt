看了新版本，整体方向非常好：这次不是“补丁式修改”，而是开始把工程往可维护结构上推了。尤其是 **JSON 解析独立、MIDI 库扫描独立、parser 测试加入、MIDI parser 补 sustain/tempo/unclosed note**，这些都属于长期收益很高的改动。

我没有在本地编译运行，下面是基于 GitHub 当前源码的静态 review。

## 已经明显改好的部分

**1. JSON PPQ 问题已修。**
 你把 JSON 解析抽成了 `JsonSheetParser`，内部固定 `JsonPpq = 480`，并用局部 `beatsToTicks()` 转换，不再依赖 `PianoController::m_ppq` 的历史状态。这个正好修掉了我上次提到的“先加载非 480 PPQ MIDI，再导入 JSON 可能 tick 错乱”的问题。

**2. Controller 轻了一点。**
 `PianoController` 现在通过 `JsonSheetParser::parseFile()` 加载 JSON，通过 `MidiLibraryService` 处理本地 MIDI 库路径、扫描和打开目录，职责比之前干净不少。 

**3. seek 后 played 状态覆盖问题已修。**
 你新增了 `resetPracticeState(bool resetStats, bool resetPlayed = true)`，`seekBeat()` 里使用 `resetPracticeState(false, false)`，然后再按当前位置设置 `note.played`，这就避免了之前 seek 后 played 被重置的问题。 

**4. 播放 tick 改成 double 累计，这个改动很正确。**
 现在 `PianoController` 有 `m_preciseTick`，`onFrame()` 用浮点 `deltaTicks` 累加，再 round 到 `m_currentTick`。这比每帧直接整数 tick 累加更稳，能减少长期播放时的累计误差。 

**5. MIDI parser 进步很大。**
 现在 `ParsedMidi` 有 `TempoEvent` / `tempos`，parser 会保留 tempo map；同时你加入了 CC64 sustain pedal 处理、按 `track:channel:note` 配对音符、未闭合 note fallback，这些都是 MIDI 文件兼容性提升的关键点。 

**6. 单元测试开始补上了。**
 CMake 已经加入 `CTest` 和 `SmartPianoQtTests`，测试覆盖了 NoteUtils、JSON parser、MIDI sustain + tempo、缺失 NoteOff fallback。这个很有价值，因为 parser 后面会越来越复杂，测试是防回归的关键。 

## 现在还建议继续修的几个点

### 1. `setSong()` 里还在用 `DefaultPpq` 初始化总长度和最大音符搜索窗口

现在 `setSong()` 里已经先设置了：

```
m_ppq = ppq > 0 ? ppq : DefaultPpq;
```

但下面仍然是：

```
m_totalTicks = DefaultPpq * 8;
m_maxNoteDurationTick = DefaultPpq * 2;
```

这对 JSON 没问题，但对 PPQ 不是 480 的 MIDI 不够严谨。建议改成：

```
m_totalTicks = m_ppq * 8;
m_maxNoteDurationTick = m_ppq * 2;
```

这能让“默认 8 拍长度”和“默认最大音符窗口 2 拍”始终跟当前曲子的 PPQ 一致。相关逻辑在当前 `setSong()` 里。

### 2. tempo map 已解析，但播放还没使用

现在 MIDI parser 已经把多个 tempo event 保存到 `ParsedMidi::tempos`，测试里也验证了 `parsed.tempos.size() == 2`。
 但 `PianoController::onFrame()` 仍然只用单一 `m_bpm` 推进：

```
const double deltaTicks =
    double(elapsedMs) * double(m_bpm) * double(m_ppq) / 60000.0;
```

所以目前是“解析了 tempo map，但播放没有按 tempo map 变速”。这不算 bug，但 README 或下一步计划里可以明确一下。下一阶段可以做一个 `PlaybackClock`：

```
class PlaybackClock {
public:
    void setTempoMap(QVector<TempoEvent> tempos, int ppq);
    qint64 advanceByMs(qint64 currentTick, qint64 elapsedMs) const;
};
```

先把 tempo map 的“时间换算”独立出来，后面播放、seek、节奏评分都会用到。当前单 BPM 推进逻辑在 `onFrame()` 里。

### 3. `result.tempos` 建议排序

`rawEvents` 后面做了排序，但 `result.tempos` 是在逐 track 解析时直接 push 的。如果是标准 format 1，tempo 通常在第一个 track，问题不大；但更稳妥的是 parse 结束前统一排序：

```
std::sort(result.tempos.begin(), result.tempos.end(),
          [](const TempoEvent &a, const TempoEvent &b) {
              return a.tick < b.tick;
          });
```

如果没有 tempo event，也可以补一个默认 tempo：

```
if (result.tempos.isEmpty()) {
    result.tempos.push_back({0, 500000}); // 120 BPM
}
```

这样 `ParsedMidi` 的语义更完整。当前 `ParsedMidi` 已经有 `tempos` 字段，但 parser 里没有看到最终排序逻辑。 

### 4. `readVarLen()` 对畸形 VLQ 可以更严格

当前 `readVarLen()` 最多读 4 字节，但如果第 4 字节仍然带 continuation bit，它也会 `return true`。MIDI VLQ 最多 4 字节，所以第 4 字节还没结束时，应该认为格式错误。

建议改成：

```
bool readVarLen(const QByteArray &data, int &offset, qint64 &value)
{
    value = 0;
    for (int i = 0; i < 4; ++i) {
        if (offset >= data.size()) return false;
        const auto byte = quint8(data.at(offset++));
        value = (value << 7) | (byte & 0x7F);
        if ((byte & 0x80) == 0) return true;
    }
    return false; // 4 字节后仍未结束，非法 VLQ
}
```

这个对正常 MIDI 没影响，但能让 parser 面对坏文件时更可靠。当前函数在 `MidiFileParser.cpp` 里。

### 5. README 的“下一步”可以更新一下

README 里“下一步”还写着“为 MIDI 解析、练习判断补单元测试”，但现在 MIDI/JSON parser 测试已经补了一部分。 
 可以改成更精确：

```
- 为 PracticeEngine / seek / 练习判定补单元测试。
- 为 MIDI parser 增加 running status、多轨、tempo map 排序、异常文件测试。
```

这样文档和当前状态更一致。

## 我建议下一版优先做什么

我会建议你下一轮按这个顺序推进：

```
1. 修 setSong() 里的 DefaultPpq 初始化问题。
2. 给 readVarLen() 加非法 VLQ 防护。
3. 给 tempo map 排序，并补默认 tempo。
4. 把 ParsedMidi::tempos 暂时传进 Song 数据结构，即使播放暂时不用。
5. 再拆 PlaybackEngine / PracticeEngine。
```

尤其是第 4 点很重要。现在 `ParsedMidi` 已经能解析 tempo map，但 `PianoController::setSong()` 只接收 title、bpm、ppq、notes，tempo 信息在加载时就丢掉了。建议下一步引入：

```
struct Song {
    QString title;
    int bpm = 120;
    int ppq = 480;
    QVector<TempoEvent> tempos;
    QVector<NoteEvent> notes;
};
```

然后：

```
void PianoController::setSong(Song song);
```

这样架构会继续变干净。

## 总体评价

这次更新质量不错。你已经把几个关键点从“能跑”推进到了“可维护”：

```
JSON parser 独立：完成
MIDI library service 独立：完成
seek / played bug：完成
播放 tick 精度：改善
MIDI parser sustain / tempo / fallback：明显增强
parser 测试：已起步
```

现在最值得继续推进的是：**把 Song / PlaybackEngine / PracticeEngine 抽出来**。一旦这三个核心模型独立，后面接 RtMidi、SQLite、MusicXML、练习报告、AI 反馈都会顺很多。