看了新提交，我的评价是：**这版质量明显上了一个台阶，可以算是从“功能 demo”进入“可维护工程雏形”了。**
 尤其是你这次不是只修单点 bug，而是把数据模型、parser 输出、测试覆盖、README 状态都一起跟进了。整体我会给 **8.5/10**；如果下一步把 `PlaybackEngine / PracticeEngine` 真正拆出来，就能到 9 分以上。

## 做得很好的地方

**1. `Song` 模型抽出来了，这是这次最重要的架构进步。**
 现在 `NoteEvent`、`TempoEvent`、`Song` 都集中在 `core/Song.h`，parser 不再各自散落一套字段，后续接 SQLite、MusicXML、练习报告都会更顺。

**2. parser 返回 `Song`，这个方向非常正确。**
 `ParsedMidi` 和 `ParsedJsonSheet` 现在都只保留 `ok / error / song`，比之前的 `bpm / ppq / notes / tempos` 平铺结构干净很多。这样 `PianoController` 只需要接收一个完整曲子对象。 

**3. 上次提到的 PPQ 初始化问题已经修到位。**
 `setSong()` 里现在用 `m_ppq * 8` 和 `m_ppq * 2` 初始化 `m_totalTicks` / `m_maxNoteDurationTick`，不再写死 `DefaultPpq`。这对非 480 PPQ 的 MIDI 更稳。

**4. MIDI parser 的健壮性提升很明显。**
 `readVarLen()` 现在对 4 字节后仍未结束的 VLQ 返回 false；tempo map 会排序，没有 tempo 时补 120 BPM 默认 tempo，晚于 0 tick 的 tempo 前也会补默认 tempo；这些都是正确处理真实 MIDI 文件时很有用的防御性逻辑。

**5. 测试覆盖已经从“有测试”进化到“有针对性的 parser 回归测试”。**
 现在测试覆盖了 JSON 默认 tempo、MIDI sustain、缺失 NoteOff、默认 tempo、跨轨 tempo 排序、晚到 tempo 前补默认 tempo、非法 VLQ。这个测试集合很实用，能防止以后改 parser 时把兼容性搞坏。

**6. README 也同步更新了。**
 现在 README 里明确写了 MIDI 解析支持 tempo map、延音踏板、缺失 NoteOff 兜底，并增加了测试命令和当前测试范围，文档状态和代码状态基本一致。

## 目前还没完全到位的地方

### 1. tempo map 已经保留，但播放仍然只按单一 BPM 推进

这是现在最大的“下一步”。
 `PianoController` 已经有 `m_tempos`，`setSong()` 也会把 song 的 tempo map 保存下来，但 `onFrame()` 里播放推进仍然是：

```
double(elapsedMs) * double(m_bpm) * double(m_ppq) / 60000.0
```

也就是说：**当前已经能解析和保存变速 MIDI 的 tempo map，但自动播放还没有按 tempo map 真实变速。** README 里也把这点列成下一步，所以状态是清楚的。 

下一步建议新增：

```
class PlaybackClock {
public:
    void setSong(const Song &song);
    qint64 advance(qint64 currentTick, qint64 elapsedMs) const;
};
```

或者更直接一点：

```
qint64 ticksFromElapsedMs(
    qint64 currentTick,
    qint64 elapsedMs,
    const QVector<TempoEvent> &tempos,
    int ppq
);
```

这样变速 MIDI 的核心逻辑能先独立出来测试，不要直接塞进 `PianoController::onFrame()`。

### 2. `PianoController` 还是有点大

虽然你已经抽了 `Song`、`JsonSheetParser`、`MidiLibraryService`，但 `PianoController` 仍然同时负责播放时钟、练习判定、音频触发、本地库加载、曲谱状态、QML 属性暴露。 

下一步最值得拆的是：

```
PlaybackEngine
  currentTick
  play / pause / stop / seek
  tempo map 推进

PracticeEngine
  expectedNotes
  noteOn 判断
  correct / wrong / missed 统计

PianoController
  只做 QML facade
```

目前 `Song` 已经抽出来了，说明拆引擎的前置条件已经具备。

### 3. MIDI format 2 建议显式拒绝

当前 MIDI header 里你读取了 trackCount 和 division，但没有读取 format 字段。标准 MIDI 的 format 0 / 1 可以按当前方式处理；但 format 2 是多个独立 sequence，不应该简单合并成同一时间线。现在如果遇到 format 2，parser 可能会错误地把多段独立曲子合并播放。

建议加：

```
const int format = qFromBigEndian<quint16>(
    reinterpret_cast<const uchar *>(payload.constData()));

if (format > 1) {
    result.error = QStringLiteral("暂不支持 MIDI Format 2 文件");
    return result;
}
```

然后补一个测试：

```
void testMidiRejectsFormat2()
```

### 4. SysEx 长度也建议做边界检查

Meta event 里你已经检查了：

```
if (length < 0 || pos + length > payload.size()) break;
```

但 SysEx 分支里目前是读完 length 后直接：

```
pos += int(length);
```

如果坏文件给了超长 SysEx length，`pos` 可能越过 payload。这个不一定立刻崩，但 parser 状态会变得不可靠。

建议改成：

```
if (length < 0 || pos + length > payload.size()) {
    result.error = QStringLiteral("MIDI SysEx 事件长度无效");
    return result;
}
pos += int(length);
```

### 5. `loadJsonSheet()` 可以避免一次大 Song 拷贝

现在这里是：

```
const ParsedJsonSheet parsed = JsonSheetParser::parseFile(path);
...
setSong(parsed.song);
```

因为 `parsed` 是 const，`parsed.song` 会被复制。大 JSON 曲谱时这会多复制一份 notes。

可以改成：

```
ParsedJsonSheet parsed = JsonSheetParser::parseFile(path);
if (!parsed.ok) {
    setStatusMessage(parsed.error);
    return;
}

const QString title = parsed.song.title;
setSong(std::move(parsed.song));
setStatusMessage(QStringLiteral("已导入 JSON：%1").arg(title));
```

这是小优化，但顺手值得改。

## 我建议你下一版的优先级

我会按这个顺序做：

```
1. MIDI parser 显式拒绝 Format 2。
2. SysEx length 加边界检查。
3. loadJsonSheet 用 std::move 避免 Song 拷贝。
4. 抽 PlaybackClock，并把 tempo map 推进逻辑写成可测试函数。
5. 再拆 PracticeEngine，把 evaluatePracticeNote 从 PianoController 里移出去。
```

其中第 4 步最关键。你现在已经把 tempo map 解析、排序、默认 tempo、Song 模型都准备好了，下一步把它真正用于播放，就能让 MIDI 文件兼容性再上一个层级。