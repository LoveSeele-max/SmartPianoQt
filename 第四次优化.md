## 1. 最优先：拆出 `PracticeEngine`

现在 `PianoController` 里还直接负责练习逻辑，比如 `evaluatePracticeNote()`、`advancePracticeTick()`、`resetPracticeState()`、`m_correctCount / m_wrongCount / m_missedCount`、`m_matchedPracticeNotes` 等。也就是说，练习模式的判定、统计、状态推进仍然和 QML facade、播放控制、音频触发混在一起。

建议新建：

```
src/practice/PracticeEngine.h
src/practice/PracticeEngine.cpp
```

核心接口可以这样设计：

```
enum class PracticeMode {
    WaitForCorrectNote,
    RhythmPractice
};

enum class JudgeType {
    Correct,
    WrongNote,
    Missed,
    Early,
    Late,
    Extra
};

struct PracticeJudgeResult {
    JudgeType type;
    int expectedMidi = -1;
    int actualMidi = -1;
    qint64 expectedTick = 0;
    qint64 actualTick = 0;
    int offsetMs = 0;
};

class PracticeEngine {
public:
    void setSong(const Song &song);
    void setMode(PracticeMode mode);
    void seek(qint64 tick);
    void reset(bool resetStats);

    QVector<NoteEvent> expectedNotes() const;
    qint64 expectedTick() const;

    QVector<PracticeJudgeResult> noteOn(int midi, qint64 currentTick);
    void advanceToNextStep();

    int correctCount() const;
    int wrongCount() const;
    int missedCount() const;
};
```

这样 `PianoController` 只需要：

```
auto results = m_practice.noteOn(midi, m_currentTick);
```

然后根据结果更新 UI 和音频。这个拆分非常关键，因为后面你要做“提前/滞后”“漏弹”“练习报告”“难点小节统计”，都不应该继续堆在 `PianoController` 里。

## 2. BPM 滑块语义建议改成“播放速度”而不是覆盖 tempo map

你现在已经把 `PlaybackClock` 接入自动播放了，`onFrame()` 会通过 `PlaybackClock::advance()` 按 tempo map 推进，这是很好的。

但现在 `setBpm()` 会直接：

```
m_tempos = PlaybackClock::tempoMapFromBpm(m_bpm);
```

这意味着：如果导入的是变速 MIDI，一旦用户拖 BPM 滑块，原始 tempo map 就会被替换成单一 BPM。

更推荐改成两个概念：

```
曲谱原始 tempo map：来自 MIDI / JSON
用户播放速度：0.5x / 0.75x / 1.0x / 1.25x
```

可以把 UI 上的 `BPM` 改成：

```
Speed
50% ~ 150%
```

代码上保留：

```
QVector<TempoEvent> m_originalTempos;
double m_playbackRate = 1.0;
```

播放时：

```
m_preciseTick = PlaybackClock::advance(
    m_preciseTick,
    qRound64(elapsedMs * m_playbackRate),
    m_originalTempos,
    m_ppq
);
```

这样变速 MIDI 的速度结构不会丢，用户也可以整体降速练习。

## 3. 抽出 `PlaybackEngine`，让 `PianoController` 只做 QML facade

你已经有了 `PlaybackClock`，它现在负责 tempo map 推进，测试也覆盖了单 tempo、跨 tempo change、晚到 tempo 等情况。  

下一步可以继续抽：

```
src/playback/PlaybackEngine.h
src/playback/PlaybackEngine.cpp
```

接口可以是：

```
class PlaybackEngine {
public:
    void setSong(const Song &song);
    void play();
    void pause();
    void stop();
    void seekTick(qint64 tick);
    void advance(qint64 elapsedMs);

    bool isPlaying() const;
    qint64 currentTick() const;
    qint64 totalTicks() const;

    QVector<int> activeAutoNotes() const;
};
```

然后 `PianoController::onFrame()` 就会变得很薄：

```
void PianoController::onFrame()
{
    const qint64 elapsedMs = m_frameClock.restart();

    m_playback.advance(elapsedMs);
    m_currentTick = m_playback.currentTick();

    refreshActiveNotes();
    emit frameChanged();
}
```

好处是播放逻辑可以完全脱离 QML 单独测试，未来支持 A-B 循环、循环小节、节拍器、变速练习都更容易。

## 4. 音频渲染线程还可以优化

现在内置钢琴合成器在 `PianoAudioDevice::readData()` 里持有 mutex，然后遍历所有 voice 渲染每个 sample；`noteOn()` 里也会在 mutex 下修改 `m_voices`，还可能 `push_back` 或 `erase`。

这在当前 demo 没问题，但实时音频里最怕：

```
音频回调持锁
音频回调中发生内存分配
UI 线程和音频线程竞争同一把锁
```

建议下一步做一个轻量命令队列：

```
enum class AudioCommandType {
    NoteOn,
    NoteOff,
    StopAll,
    SetVolume
};

struct AudioCommand {
    AudioCommandType type;
    int midi = 0;
    int velocity = 0;
    int volume = 0;
};
```

UI 线程只把命令 push 到队列，音频线程在 `readData()` 开头消费命令。这样可以减少 UI 和音频线程直接竞争。

更进一步，`m_voices.reserve(128)`，并且不要在 audio callback 里 `erase` 大段 vector，可以改成标记 inactive，然后定期压缩。

## 5. QML 应该拆文件

`Main.qml` 现在仍然是一个大文件：顶部状态栏、控制面板、本地 MIDI 列表、PianoRoll 容器、虚拟键盘、StatBox 都在里面。

建议拆成：

```
qml/Main.qml
qml/components/TopBar.qml
qml/components/ControlPanel.qml
qml/components/StatBox.qml
qml/components/LocalMidiPanel.qml
qml/components/VirtualKeyboard.qml
qml/components/KeyboardKey.qml
qml/components/TransportControls.qml
```

这样后续加“设置页”“MIDI 设备选择”“练习报告页”“曲谱库页”时不会把 `Main.qml` 越写越长。

当前 QML 里还有 `isBlackMidi()`、`noteName()`、`whiteIndexBefore()`、`keyX()` 这些 JS 函数，C++ 里也有类似逻辑。可以把键盘布局抽成 C++ 或 QML 单独工具，避免 UI 和 C++ 各维护一套音符逻辑。

## 6. 虚拟键盘可以做成自绘 Item

现在虚拟键盘用 `Repeater` 生成 61 个 Rectangle + Text + MouseArea。对 61 键来说没问题，但如果以后做 88 键、缩放、触控、多点输入、MIDI 可视化，建议像 `PianoRollItem` 一样做一个 C++ 自绘 `KeyboardItem`。

大致结构：

```
src/ui/KeyboardItem.h
src/ui/KeyboardItem.cpp
```

属性：

```
Q_PROPERTY(QVariantList activeNotes READ activeNotes WRITE setActiveNotes NOTIFY activeNotesChanged)
Q_PROPERTY(QVariantList expectedNotes READ expectedNotes WRITE setExpectedNotes NOTIFY expectedNotesChanged)
Q_PROPERTY(int firstMidi READ firstMidi WRITE setFirstMidi NOTIFY rangeChanged)
Q_PROPERTY(int lastMidi READ lastMidi WRITE setLastMidi NOTIFY rangeChanged)
```

信号：

```
signals:
    void notePressed(int midi);
    void noteReleased(int midi);
```

这样键盘绘制和鼠标命中都能集中到 C++，QML 结构会更轻。

## 7. `QVariantList` 后续建议改成 Model

现在 `notes`、`activeNotes`、`expectedNotes`、`localMidiFiles` 都是通过 `QVariantList` 暴露给 QML。`notes()` 每次会把全部 `m_notes` 转成 QVariantMap 列表；曲子变大时，这个开销会比较明显。

建议分两步：

第一步保留当前接口，但减少 `notes` 在 QML 中的使用，因为 `PianoRollItem` 已经直接读 C++ 的 `noteEvents()`，这点是好的。

第二步改成：

```
class LocalMidiFileModel : public QAbstractListModel
class NoteListModel : public QAbstractListModel
```

`localMidiFiles` 最适合先改，因为它直接给 `ListView` 用。QML `ListView` 用 model 比 `QVariantList` 更标准，也方便以后加搜索、排序、收藏、最近打开。

## 8. 本地曲谱库建议从文件夹升级为 SQLite

当前本地库是扫描 `midi_library` 目录，简单实用。

下一步想做“智能练琴”，还是建议落 SQLite：

```
sheets(
    id INTEGER PRIMARY KEY,
    title TEXT,
    file_path TEXT,
    source_format TEXT,
    file_hash TEXT UNIQUE,
    bpm INTEGER,
    ppq INTEGER,
    created_at TEXT,
    updated_at TEXT
);

practice_sessions(
    id INTEGER PRIMARY KEY,
    sheet_id INTEGER,
    started_at TEXT,
    duration_seconds INTEGER,
    mode TEXT,
    correct_count INTEGER,
    wrong_count INTEGER,
    missed_count INTEGER,
    score INTEGER
);

practice_events(
    id INTEGER PRIMARY KEY,
    session_id INTEGER,
    expected_midi INTEGER,
    actual_midi INTEGER,
    expected_tick INTEGER,
    actual_tick INTEGER,
    result TEXT,
    offset_ms INTEGER
);
```

这样后续“练习报告”“常错小节”“连续正确后自动提速”“错多了自动降速”才有数据基础。

## 9. MIDI 输入是下一个核心功能

README 里下一步仍然写着接入 RtMidi，并迁移 Yamaha PSR-E383 的 `0xFE/0xF8` 过滤。

这个功能优先级很高，因为项目叫智能钢琴，真实键盘输入会直接决定练习模式体验。

建议加：

```
src/midi/MidiInputService.h
src/midi/MidiInputService.cpp
```

接口：

```
class MidiInputService : public QObject {
    Q_OBJECT
public:
    QStringList inputPorts() const;
    Q_INVOKABLE void refreshPorts();
    Q_INVOKABLE void openPort(int index);
    Q_INVOKABLE void close();

signals:
    void portsChanged();
    void noteOn(int midi, int velocity);
    void noteOff(int midi);
    void statusChanged(QString message);
};
```

过滤逻辑：

```
if (status == 0xFE || status == 0xF8) {
    return;
}
```

然后 UI 左侧加一个 MIDI 设备选择框，比鼠标点虚拟键盘更接近真实练琴。

## 10. MIDI parser 还可以继续增强

你现在 MIDI parser 已经做了 tempo map、sustain pedal、缺失 NoteOff、Format 2 拒绝、SysEx length 检查，这些已经不错。

下一步可以补：

```
Time Signature meta event，0x58
Key Signature meta event，0x59
Program Change，区分钢琴轨和鼓轨
Channel 10 鼓轨过滤或单独标记
CC7 volume
CC10 pan
CC11 expression
歌词/marker/cue point，用于显示段落
```

尤其是 **Channel 10 鼓轨**：很多 MIDI 文件会有鼓轨，如果直接显示成钢琴音符，卷帘窗会很奇怪。可以先简单过滤：

```
if (channel == 9) {
    continue; // MIDI channel 10, percussion
}
```

或者给 `NoteEvent` 加：

```
bool percussion = false;
```

UI 里默认隐藏鼓轨。

## 11. 测试可以继续从 parser 扩到 practice / playback / seek

现在 CMake 已经把 `PlaybackClock` 和 parser tests 加入测试目标了，`ctest` 能跑核心测试。 

下一批测试建议：

```
PracticeEngine:
  单音弹对
  单音弹错
  和弦只弹一部分不推进
  和弦全部弹对才推进
  重复按同一个正确音不重复加 correct
  seek 后 expected tick 正确

PlaybackEngine:
  stop 后 currentTick 为 0
  seek 后 active notes 刷新
  播放到末尾自动停止
  A-B loop，未来可测

MidiInputService:
  过滤 0xFE / 0xF8
  NoteOn velocity 0 视为 NoteOff
```

这会让你后续重构更放心。

## 12. CI 和打包可以开始补

现在已有 CTest，但还没看到 GitHub Actions workflow。CMake 里已经把测试目标组织好了，适合接 CI。

建议加：

```
.github/workflows/windows.yml
```

做三件事：

```
1. 配置 Qt
2. cmake configure/build
3. ctest --output-on-failure
```

Windows 打包后续可以加：

```
windeployqt SmartPianoQt.exe
```

并准备：

```
deploy/
  package-windows.ps1
  README-runtime.md
```

这样别人下载 release 后不用自己配 Qt 环境。

## 13. 小的代码建议

`PlaybackClock::tempoIndexForTick()` 现在是线性扫描 tempo map。tempo event 通常不多，所以没问题；如果想更严谨，可以换成 `std::upper_bound`。

`PlaybackClock::normalizedTempoMap()` 没有合并同 tick 的多个 tempo。实际 MIDI 里同 tick 多个 tempo 可能出现，通常最后一个更合理。可以考虑归并同 tick tempo。

`PianoController::setBpm()` 现在会覆盖 tempo map，这个前面提过，建议改成 playback rate。

`Main.qml` 里颜色、间距、尺寸写死较多，后续可以抽一个 `Theme.qml`：

```
QtObject {
    readonly property color panelBg: "#18181b"
    readonly property color border: "#2f3036"
    readonly property color accent: "#14b8a6"
}
```

这样 UI 风格更容易统一。

## 我建议的下一轮路线

优先级我会这样排：

```
第一优先级：
1. 拆 PracticeEngine
2. 把 BPM 滑块改成 Speed / playbackRate
3. 给 PracticeEngine 补测试

第二优先级：
4. 拆 PlaybackEngine
5. 接 RtMidi 输入
6. 增加 MIDI 设备选择 UI

第三优先级：
7. 本地曲谱库 SQLite
8. 练习报告
9. QML 拆组件
10. LocalMidiFiles 改 QAbstractListModel
```