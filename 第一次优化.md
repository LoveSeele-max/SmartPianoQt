我建议按下面优先级优化。

## 1. 先修两个小逻辑坑

**第一个是 JSON 曲谱的 PPQ 转换问题。**
 `loadJsonSheet()` 里 `note.startTick = beatToTick(startBeat)`、`durationTick = beatToTick(durationBeat)` 会使用当前对象里的 `m_ppq`；但后面 `setSong(title, bpm, DefaultPpq, parsed)` 又把 JSON 曲谱的 PPQ 固定成 `DefaultPpq`。如果你先加载了一个 PPQ 不是 480 的 MIDI，再导入 JSON，JSON 的 tick 可能会按旧 PPQ 算错。相关逻辑在 JSON 解析和 `setSong()` 里都能看到。

可以改成局部固定 PPQ：

```
namespace {
qint64 beatsToTicks(double beats, int ppq)
{
    return qRound64(beats * ppq);
}
}

void PianoController::loadJsonSheet(const QString &path)
{
    constexpr int jsonPpq = DefaultPpq;

    // ...

    note.startTick = beatsToTicks(startBeat, jsonPpq);
    note.durationTick = qMax<qint64>(
        jsonPpq / 8,
        beatsToTicks(durationBeat, jsonPpq)
    );

    // ...

    setSong(title, bpm, jsonPpq, parsed);
}
```

**第二个是 seek 后的 `played` 状态会被覆盖。**
 `seekBeat()` 里先把 `note.played` 设为 `note.startTick < m_currentTick`，但随后调用 `resetPracticeState(false)`，而 `resetPracticeState()` 不管 `resetStats` 是不是 false，都会把所有 `note.played` 设回 false。这样 seek 后瀑布流里“已播放”的视觉状态可能不符合预期。

可以把 `resetPracticeState(false)` 放到设置 `played` 前面，或者给它加一个参数：

```
void resetPracticeState(bool resetStats, bool resetPlayed = true);
```

然后 seek 时：

```
resetPracticeState(false, false);
for (auto &note : m_notes) {
    note.played = note.startTick < m_currentTick;
}
```

## 2. 把 `PianoController` 拆小

现在 `PianoController` 同时负责曲谱导入、本地 MIDI 库扫描、播放时钟、练习判定、状态统计、音频触发和 QML 暴露。`loadSheet()`、`refreshLocalMidiLibrary()`、`onFrame()`、`evaluatePracticeNote()` 这些职责都集中在一个类里，短期方便，后期会越来越难扩展。

建议拆成：

```
core/Song.h
core/NoteEvent.h

playback/PlaybackEngine.h/.cpp
  play()
  pause()
  stop()
  seekTick()
  currentTick()
  setBpm()

practice/PracticeEngine.h/.cpp
  setSong()
  setMode()
  expectedNotes()
  onUserNoteOn()
  judgeResult()

parser/JsonSheetParser.h/.cpp
parser/MidiFileParser.h/.cpp

library/MidiLibraryService.h/.cpp

app/PianoController.h/.cpp
  只作为 QML facade，转发属性和信号
```

这样做的好处是：以后接入真实 MIDI 输入、节奏评分、A-B 循环、SQLite、AI 练习报告时，不需要继续把所有逻辑塞进一个类。

## 3. 播放时间轴建议改成“绝对时间 + 浮点 tick”

现在播放推进依赖 16ms `QTimer`，每帧根据 `elapsedMs` 增加 tick。这个 MVP 可用，但长曲播放、变速、拖动进度、后续 tempo map 都会更稳，如果改成“播放开始时记录 anchor tick + elapsed time 计算当前 tick”。当前 `onFrame()` 的逐帧推进逻辑在源码里很清楚。

可以先做最小改动：增加一个 double 精度的 tick 累加器，避免每帧整数四舍五入带来的漂移。

```
double m_preciseTick = 0.0;

void PianoController::playPause()
{
    // 开始播放前
    m_preciseTick = double(m_currentTick);
    m_frameClock.restart();
}

void PianoController::onFrame()
{
    const qint64 elapsedMs = m_frameClock.restart();

    const double deltaTicks =
        double(elapsedMs) * double(m_bpm) * double(m_ppq) / 60000.0;

    m_preciseTick += deltaTicks;
    m_currentTick = qBound<qint64>(
        0,
        qRound64(m_preciseTick),
        m_totalTicks
    );

    // ...
}
```

更进一步，可以让 `PlaybackEngine` 内部维护 tempo map，`currentTick()` 由绝对时间换算出来，而不是每帧直接改状态。

## 4. 练习模式可以从“对/错”升级到“判定结果流”

现在 `evaluatePracticeNote()` 主要判断当前 MIDI 是否在 expected set 里：命中就加 `correctCount`，不命中就加 `wrongCount`。这适合“等待弹对才继续”的第一版，但 `missedCount` 目前还没有真正的判定来源。

建议引入一个结构：

```
enum class JudgeType {
    Correct,
    WrongNote,
    Early,
    Late,
    Missed,
    Extra
};

struct JudgeResult {
    JudgeType type;
    int expectedMidi = -1;
    int actualMidi = -1;
    qint64 expectedTick = 0;
    qint64 actualTick = 0;
    int timingOffsetMs = 0;
};
```

然后分两种模式：

```
等待模式：
  弹对当前和弦才推进
  不统计 missed，或者隐藏 missed

节奏模式：
  时间继续走
  超过容忍窗口还没弹，统计 missed
  弹早/弹晚记录 timingOffsetMs
```

这一步会让“智能钢琴”的价值明显提升，因为你后面可以生成练习报告：哪些小节经常错、哪些音经常慢、哪几个和弦需要循环练。

## 5. MIDI 解析建议补 tempo map、踏板、多轨边界

你现在的 MIDI parser 已经处理了 SMF 头、PPQ、running status、Note On/Off，并把事件配对成 `NoteEvent`，作为第一版够用。

但现在有几个明显扩展点：

1. **tempo 只取第一个。**
    代码里 `tempoSeen` 后就不再处理后续 tempo，所以变速 MIDI 会按单一 BPM 播放。建议新增：

   ```
   struct TempoEvent {
       qint64 tick = 0;
       int microsecondsPerQuarter = 500000;
   };
   ```

2. **踏板 CC64 没处理。**
    钢琴 MIDI 里 sustain pedal 很常见，不处理会导致音符长度偏短。可以先把 CC64 事件解析出来，再在配对 NoteOff 时根据踏板状态延长 duration。

3. **active note key 建议包含 track。**
    现在配对 key 是 channel + note。如果两个轨道同 channel 同音高有重叠，理论上可能错配。更稳的是：

   ```
   QString key = QStringLiteral("%1:%2:%3")
       .arg(event.track)
       .arg(event.channel)
       .arg(event.note);
   ```

4. **未闭合音符可以兜底。**
    有些 MIDI 文件缺 NoteOff。解析结束后可以把仍 active 的 note 延长到 track 结束或给一个默认时值，避免整首曲子丢音。

## 6. 尽快接真实 MIDI 输入

README 的“下一步”里也写了接入 RtMidi，这是我认为最该做的功能之一。因为你的项目叫“智能钢琴”，真正的外接电钢琴输入比本地鼠标键盘更核心。

可以加一个 `MidiInputService`：

```
class MidiInputService : public QObject {
    Q_OBJECT
public:
    QStringList inputPorts() const;
    void openPort(int index);
    void close();

signals:
    void noteOn(int midi, int velocity);
    void noteOff(int midi);
    void deviceStatusChanged(QString text);
};
```

并保留 Yamaha/电子琴常见实时消息过滤：

```
const unsigned char status = msg[0];

if (status == 0xFE || status == 0xF8) {
    return; // Active Sensing / Timing Clock
}
```

然后连接到现有控制器：

```
connect(midiInput, &MidiInputService::noteOn,
        &controller, &PianoController::noteOn);

connect(midiInput, &MidiInputService::noteOff,
        &controller, &PianoController::noteOff);
```

## 7. 音频后端再工程化一点

你现在已经做了两层音源：优先找 SoundFont + FluidSynth，找不到就走内置钢琴合成器，这个方向很好。源码里 `open()` 也是先 `openFluidSynth()`，失败后 `openInternalPiano()`。

建议继续优化三点：

**第一，拆接口。**

```
class AudioEngine {
public:
    virtual ~AudioEngine() = default;
    virtual void noteOn(int midi, int velocity) = 0;
    virtual void noteOff(int midi) = 0;
    virtual void stopAll() = 0;
    virtual void setVolume(int volume) = 0;
};
```

然后：

```
FluidSynthEngine
InternalPianoEngine
```

`MidiSynth` 只负责选择后端。

**第二，减少音频回调里的锁和分配。**
 内置合成器在 `readData()` 里渲染声音，实时音频路径尽量避免长期锁、内存分配和复杂逻辑。可以用一个简单 command queue：UI 线程只投递 noteOn/noteOff 命令，音频线程在 buffer 开始时批量消费。

**第三，去掉硬编码 FluidSynth 路径。**
 当前解析 FluidSynth DLL 时有若干本机路径兜底，包括 E 盘、C 盘、MSYS 路径等。
 发布版建议只保留：

```
1. FLUIDSYNTH_DLL
2. FLUIDSYNTH_PREFIX
3. exe 同目录
4. 系统 PATH
```

Windows 下也尽量避免长期使用全局 `SetDllDirectoryW`，更适合把 DLL 放在 exe 同目录，或者使用更局部的 DLL 搜索目录策略。

## 8. QML 暴露方式可以更类型安全

`main.cpp` 里现在通过 `rootContext()->setContextProperty("piano", &controller)` 暴露控制器，同时注册了 `PianoRollItem`。这能跑，但 QML 工具链对 `piano` 的类型推断会弱一些。

可以改成单例注册：

```
qmlRegisterSingletonInstance(
    "SmartPianoQt",
    1, 0,
    "PianoController",
    &controller
);
```

QML 里：

```
import SmartPianoQt 1.0

Button {
    text: PianoController.playing ? "暂停" : "播放"
    onClicked: PianoController.playPause()
}
```

同时可以把 `Main.qml` 拆成：

```
Main.qml
ControlPanel.qml
PianoRollPanel.qml
VirtualKeyboard.qml
LocalMidiList.qml
StatBox.qml
```

现在单个 `Main.qml` 还不算失控，但后续加 MIDI 设备选择、设置页、练习报告后会变得很长。

## 9. 大曲谱建议用 Model，不要长期传大 QVariantList

当前 `notes` 是 QML 属性，C++ 端会把所有音符转成 `QVariantList`。这对 demo 没问题，但大 MIDI 文件有几千到几万音符时，反复构造 QVariant 会比较浪费。你的瀑布流本身已经直接从 C++ `noteEvents()` 读取并只绘制可见音符，这是好的；后续 UI 列表如果要显示音符，建议用 `QAbstractListModel`。

可以这样划分：

```
PianoRollItem:
  继续直接读 const QVector<NoteEvent>&
  只画可见范围

QML 列表/调试面板:
  用 NoteListModel
```

你的自绘 PianoRoll 方向是对的，比给每个音符创建 QML 控件更稳。

## 10. 构建、测试和发布可以补起来

现在 CMake 已经能构建 Qt Quick/QML 模块和 C++ 源码，但还没有测试目标，install 也比较基础。
 README 里的运行方式偏本机路径，比如 Qt、MinGW、FluidSynth 路径都依赖本地环境。

建议下一轮加这些：

```
CMakePresets.json
tests/
  test_noteutils.cpp
  test_json_parser.cpp
  test_midi_parser.cpp
  test_practice_engine.cpp
GitHub Actions:
  Windows build
  artifact upload
deploy/
  windeployqt script
```

最值得测的用例：

```
1. C4 <-> MIDI 60 转换
2. JSON 在不同历史 PPQ 下导入仍然正确
3. MIDI running status
4. MIDI tempo event
5. 同一 tick 多音和弦练习判定
6. 错音不会推进练习 tick
7. seek 后 played 状态正确
```